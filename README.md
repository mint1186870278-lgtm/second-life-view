# Second Life View Frontend

Phase 1A contains the Vite/React/TypeScript application skeleton and canonical domain/data foundation. Product pages are development placeholders only.

```bash
npm install
npm run dev
npm run test
npm run build
```

Route strings in `src/app/routes.ts` are implementation choices, not Product Authority. W02-A and W02-B remain internal W02 views.
