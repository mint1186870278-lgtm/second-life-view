import { createEmptyFixtureGraph } from './builders'

export const emptyProjectGraph = createEmptyFixtureGraph({
  project_id: 'fixture-project-empty',
  name: 'Empty architecture fixture',
  region: '',
  project_type: '',
  project_stage: '',
})
